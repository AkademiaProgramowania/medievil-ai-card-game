package medevil.ai.client;

public class GuiParams {

    final static int WIDTH = 960;
    final static int HEIGHT = 540;
    final static  int CARD_WIDTH = (int) (WIDTH * 0.2);
    final static  int CARD_HEIGHT = (int) (HEIGHT * 0.55);
}
